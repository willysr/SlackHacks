wxGTK3 is part of wxWidgets, a cross-platform API for writing GUI
applications on multiple platforms that still utilize the native platform's
controls and utilities.

This package build unicode by default. If you do not want the unicode build, use:
  UNICODE=NO ./wxGTK3.SlackBuild

wxGTK3 can be installed alongside with wxGTK.

webkitgtk3 is an optional dependency of this package.